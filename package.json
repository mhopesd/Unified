{
  "name": "unified-open-world",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "start": "vite --host --open"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
